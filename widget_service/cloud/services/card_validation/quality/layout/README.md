# 布局安全错误样例

这些 JSON 文件保存可复现的 artifact 片段，用于人工复核和回归测试。
布局安全校验器只报告能够确定的同一父组件下兄弟组件矩形重叠，不推导最小尺寸、主轴容量或字号修复建议。

融球场景从 `fusionBallBackground` 开始忽略整棵背景子树，只检查前景内容树；Stack 默认允许叠放，只有包含内容组件的分支参与候选比较。

历史 `linear-overflow-*.json` 文件保留作为旧行为记录，不再代表当前校验器的错误类型。
这些文件是错误输入样例，不是线上 artifact 产物。
